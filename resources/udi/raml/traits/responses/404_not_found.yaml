responses:
  #Используется когда запрашиваемый ресурс не найден, либо, вследствии каких либо причн не должен быть возвращен запрашиваему клиенту
  404:
    description: Ресурс <<resourcePath>> не найден
    body:
      application/json:
        type: Response
