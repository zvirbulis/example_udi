responses:
  # Ошибка, которая возникает при наличии какой либо конфликтной ситуации.
  # Например, при дублировании записи, попытке удаления корневого объекта, перевод в неподходящий статус
  409:
    description: Конфликтная ситуация <<resourcePath>>
    body:
      application/json:
        type: Response
